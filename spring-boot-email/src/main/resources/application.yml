server:
  port: 8802

spring:
  mail:
    host: smtp.qq.com
    username: 2457081614@qq.com
    # 这个不是邮件密码，是授权码
    password: gdpjoadesczudije
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
  thymeleaf:
    enabled: true
    mode: HTML5
    encoding: UTF-8
    prefix: classpath:/templates/ # 模板存放在资源目录的 templates/ 文件下
    suffix: .html # 模板后缀
    check-template-location: true
    check-template: false
    cache: false # 调试时关闭缓存